# Minimal dependencies for scheduler service
# The scheduler only enqueues jobs - it doesn't call ad platform APIs
SQLAlchemy==2.0.34
psycopg2-binary==2.9.9
python-dotenv==1.0.1
redis==5.0.1
rq==1.15.1
