# Minimal dependencies for scheduler service
# The scheduler only runs cron jobs that enqueue work to the ARQ worker
SQLAlchemy==2.0.34
psycopg2-binary==2.9.9
python-dotenv==1.0.1
arq==0.26.1
sentry-sdk==1.39.1
